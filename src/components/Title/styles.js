import styled from "styled-components";

export const Title = styled.h2`
    margin-top: ${props => props.theme === 'nopx' ? '0px' : '30px'};
    color: white;
    text-align: center;
    font-size: 38px;
    font-style: normal;
    font-weight: 600;
    

`